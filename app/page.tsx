"use client";

import styled from "@emotion/styled";

const Home = () => (
  <main>
    <section>
      <AccrentText>Home</AccrentText>
    </section>
  </main>
);

const AccrentText = styled.div`
  color: skyblue;
`;

export default Home;
